[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kb-util-func-finder"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
kbrank = "kbrank:main"

[tool.hatch.build.targets.wheel]
only-include = ["kbrank.py"]
